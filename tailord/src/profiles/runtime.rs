
pub struct ProfileRuntime {
    pub fan_handles: Vec<FanRuntimeHandle>,
    pub led_handles: Vec<LedRuntimeHandle>,
    pub performance_profile_handle: Option<PerformanceProfileRuntimeHandle>,
}

pub struct ProfileRuntimeHandle {
    
}